# Testing

Run `swift test`. Coverage includes JSON decoding, validator target/range/probe behavior, session persistence, and snapshot diffs. Test live adapters against a disposable Logic project and confirm a fresh read-only scan after every action. Never use a production project as an automation fixture.
