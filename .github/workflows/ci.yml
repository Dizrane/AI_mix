name: CI

on:
  pull_request:
  push:
    branches: [main]

jobs:
  test:
    name: Build and test (macOS)
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v4
      - name: Swift version
        run: swift --version
      - name: Run test suite
        run: swift test
